# =============================================================================
# KGFLM Pipeline Configuration
# =============================================================================

project:
  name: "kgflm-traffic-annotation"
  version: "0.1.0"
  description: "Knowledge Graph Guided Foundation Language Model for Traffic Annotation"
  author: "Brileena Benny"

# =============================================================================
# Paths
# =============================================================================

paths:

  project_root: "."

  random_frames: "random_frames"

  ontology: "annotation_pipeline/common/ontology/traffic_ontology_v2.json"

  prompts: "annotation_pipeline/common/prompts"

  outputs: "annotation_pipeline/vlm_first/outputs"

  scene_cache: "annotation_pipeline/vlm_first/outputs/scene_cache"

  pipeline_cache: "annotation_pipeline/vlm_first/outputs/pipeline_cache"

  visualizations: "annotation_pipeline/vlm_first/outputs/visualizations"

  masks: "annotation_pipeline/vlm_first/outputs/masks"

  annotations: "annotation_pipeline/vlm_first/outputs/annotations"

  logs: "annotation_pipeline/vlm_first/outputs/logs"

# =============================================================================
# Models
# =============================================================================

models:

  # --------------------------------------------------------------------------
  # Scene Understanding
  # --------------------------------------------------------------------------

  scene_understanding:

    backend: smolvlm

    models:

      gemma:

        model_id: "google/gemma-3-4b-it"

        device: "cuda"

        dtype: "bfloat16"

        attention: "flash_attention_2"

        trust_remote_code: false

        compile: false

        max_new_tokens: 2048

        temperature: 0.1

      smolvlm:

        model_id: "HuggingFaceTB/SmolVLM2-500M-Video-Instruct"

        device: "cpu"

        dtype: "float32"

        attention: "sdpa"

        trust_remote_code: false

        compile: false

        max_new_tokens: 2048

        temperature: 0.1

  # --------------------------------------------------------------------------
  # Grounding
  # --------------------------------------------------------------------------

  grounding:

    backend: locate_anything

    models:

      locate_anything:

        model_id: "nvidia/LocateAnything-3B"

        device: "cuda"

        coordinate_scale: 1000

  # --------------------------------------------------------------------------
  # Segmentation
  # --------------------------------------------------------------------------

  segmentation:

    backend: sam2

    models:

      sam2:

        model_id: "sam2.1_hiera_large"

        device: "cuda"

        checkpoint: null

        config: null

        multimask_output: false

# =============================================================================
# Pipeline
# =============================================================================

pipeline:

  confidence_threshold: 0.30

  max_objects_per_image: 50

  save_intermediate_cache: true

  save_pipeline_cache: true

  save_raw_outputs: false

  save_visualizations: true

  benchmark: false

  supported_image_extensions:

    - ".png"
    - ".jpg"
    - ".jpeg"
    - ".bmp"
    - ".tiff"

# =============================================================================
# Logging
# =============================================================================

logging:

  level: "INFO"

  log_to_file: true

  log_filename: "pipeline.log"

  format: "%(asctime)s | %(name)-35s | %(levelname)-8s | %(message)s"

  date_format: "%Y-%m-%d %H:%M:%S"